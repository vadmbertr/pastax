theme:
  name: material
  features:
    - navigation.sections
    - toc.integrate
    - header.autohide
  palette:
    - scheme: default
      primary: white
      accent: amber
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - scheme: slate
      primary: black
      accent: amber
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode
  icon:
    repo: fontawesome/brands/github # GitHub logo (top right)
  # logo: "_static/logo.png"  # package logo (top left)
  # favicon: "_static/favicon.png"  # package favicon

site_name: sealagrangiax
site_description: The documentation for the sealagrangiax package.
site_author: Vadim Bertrand
site_url: https://sealagrangiax.readthedocs.io/en/latest/

repo_url: https://github.com/vadmbertr/sealagrangiax
repo_name: vadmbertr/sealagrangiax
edit_uri: ""  # no edit button

# strict: true

extra_javascript:
  - _static/mathjax.js
  - https://unpkg.com/mathjax@3/es5/tex-mml-chtml.js

markdown_extensions:
  - pymdownx.arithmatex:
      generic: true
  - pymdownx.superfences  # enable syntax highlighting
  - pymdownx.details  # allow hidden expandable regions denoted by ???
  # - pymdownx.include  # include one Markdown file into another
  - admonition
  - toc:
      # permalink: "¤"  # adds a clickable permalink to each section heading
      permalink: true
      toc_depth: 3
  # - codehilite:
  #     guess_lang: false
  #     linenums: true
  #     style: monokai

plugins:
  - search  # default search plugin; needs manually re-enabling when using any other plugins
  - autorefs  # cross-links to headings
  - include_exclude_files:
      exclude:
        - "_overrides"
  - include-markdown
  - mknotebooks  # Jupyter notebooks
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          setup_commands:
            - import pytkdocs_tweaks
            - pytkdocs_tweaks.main()
            - import jaxtyping
            - jaxtyping.set_array_name_format("array")

          options:
            allow_inspection: true
            docstring_style: numpy
            heading_level: 2
            inherited_members: true  # allow looking up inherited methods
            members_order: source  # order methods according to their order of definition in the source code
            filters:
            - "!^_"
            show_bases: true
            show_if_no_docstring: true
            show_inheritance_diagram: true
            show_root_full_path: true  # display "foo.bar" not just "bar"
            show_root_heading: true  # actually display anything at all...
            show_signature_annotations: true
            show_signature_return_annotation: true
            show_signature_parameter_types: true
          external:
            numpy: https://numpy.org/doc/stable/

nav:
  - "index.md"
  - API:
      - "api/simulator.md"
      - "api/trajectory.md"
      - "api/grid.md"
      - "api/evaluation.md"
      - "api/utils.md"
  # - Examples:
  #     - "tutorials/diffrax.md"